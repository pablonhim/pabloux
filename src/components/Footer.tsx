export function Footer() {
  return (
    <footer className="border-t border-border px-6 py-6 text-center font-mono text-[11px] text-text-muted">
      © {new Date().getFullYear()} Nhim Chanborey — Product & AI Strategy
    </footer>
  )
}
